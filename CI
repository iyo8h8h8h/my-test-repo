name: CI
on: 
  push:
  workflow_dispatch: # This is the magic line that allows manual runs

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run script
        run: echo "GitHub Actions is now active!"

